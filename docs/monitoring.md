# Monitoring

Prometheus + Grafana monitoring stack, enabled via `COMPOSE_PROFILES=monitoring`.

Dashboard JSONs are in `deploy/monitoring/grafana/provisioning/dashboards/`.
See the root `README.md` for full setup instructions.
